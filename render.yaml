services:
  # CI worker (kept as-is, explicit plan for determinism)
  - type: worker
    name: hail-o-ci
    env: docker
    plan: starter
    dockerfilePath: Dockerfile.ci
    branch: main
    autoDeploy: true

  # Production API
  - type: web
    name: hail-o-api
    env: docker
    plan: free
    dockerfilePath: backend/Dockerfile
    branch: main
    autoDeploy: true
    healthCheckPath: /api/healthz
    envVars:
      - key: ENV
        value: production
      - key: BACKEND_DB_MODE
        value: postgres
      - key: DATABASE_URL
        fromDatabase:
          name: hail-o-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true

  # Staging API (separate DB)
  - type: web
    name: hail-o-api-staging
    env: docker
    plan: free
    dockerfilePath: backend/Dockerfile
    branch: main
    autoDeploy: true
    healthCheckPath: /api/healthz
    envVars:
      - key: ENV
        value: staging
      - key: BACKEND_DB_MODE
        value: postgres
      - key: DATABASE_URL
        fromDatabase:
          name: hail-o-db-staging
          property: connectionString
      - key: JWT_SECRET
        generateValue: true

databases:
  # Production DB (keep paid if you want; change to "free" if billing blocks provisioning)
  - name: hail-o-db
    databaseName: hailo
    user: hailo
    plan: basic-256mb

  # Staging DB (free to ensure it provisions without billing friction)
  - name: hail-o-db-staging
    databaseName: hailo_staging
    user: hailo_staging
    plan: free
