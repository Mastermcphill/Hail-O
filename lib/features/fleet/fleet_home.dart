import 'package:flutter/material.dart';

class FleetHome extends StatelessWidget {
  const FleetHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Fleet Dashboard'),
    );
  }
}
